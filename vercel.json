{
  "crons": [
    // Vercel Cron Job - Geçici olarak devre dışı (GitHub Actions kullanılıyor)
    // Geri almak için comment'leri kaldır
    // {
    //   "path": "/api/cron-sync",
    //   "schedule": "* * * * *"
    // }
  ],
  "functions": {
    "api/cron-sync.js": {
      "maxDuration": 300
    }
  }
}
